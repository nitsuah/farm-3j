version: '3.8'
services:
  farm-app:
    image: node:22-alpine
    working_dir: /app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    entrypoint: ["sh", "/app/start-dev.sh"]

volumes:
  node_modules:
